# 🎧 Modern Music Player Pro (CustomTkinter + Pygame)

An upgraded version of my earlier music player with a **modern UI**, **dark/light mode**, and **volume/progress bar** support.

## ✨ Features
- Modern UI using **CustomTkinter**
- Dark/Light mode toggle 🌙☀️
- Volume control & progress bar
- Real-time playback updates
- Uses `pygame` for audio playback
- Automatically downloads control icons

## 🧩 Technologies Used
- Python
- CustomTkinter
- Pygame
- Mutagen (for song duration)
- Requests

## ▶️ How to Run
1. Install dependencies:
   ```bash
   pip install customtkinter pygame mutagen requests
   ```
2. Save and run:
    ```bash
    python modern_music_player_2.py

    ```

3. Choose a folder with MP3 files and enjoy!

## 🧠 What I Learned

- GUI modernization with CustomTkinter

- Handling song progress dynamically

- Combining aesthetic UI with functional logic